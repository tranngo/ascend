const CalendarHeader = ({ label }) => (
  <div>
    <div
      className="uppercase font-medium text-gray-600 text-xs"
      style={{ color: "#70757a" }}
    >
      {label}
    </div>
  </div>
);

export default CalendarHeader;
